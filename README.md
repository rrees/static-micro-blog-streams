# static-micro-blog-streams
Re-implementation of the blog streams idea using static generation

## Running

```
npm install
just serve
```

## Metadata usage

Use `topic: true` on an index page to expose that page to the topic list on the root page